extends layout

block content
    .container
        h1 Applicants
        ul#applications.list-unstyled
            for applicant in applicants
                li
                    h3
                        a(href="/#{applicant._id}") #{applicant.name}
                    a(href="/applicant/remove/#{applicant._id}") Remove